# Nimbus and zookeeper hosts to be provided by Roboconf
# (imported on worker node)
#nimbus.host: "localhost"
#storm.zookeeper.servers:
#    - "localhost"
java.library.path: "/usr/local/lib"
storm.local.dir: "/var/log/storm"
supervisor.slots.ports:
    - 6700
    - 6701
    - 6702
    - 6703
    - 6704
#storm.scheduler: "your.scheduler.SchedulerClass"
