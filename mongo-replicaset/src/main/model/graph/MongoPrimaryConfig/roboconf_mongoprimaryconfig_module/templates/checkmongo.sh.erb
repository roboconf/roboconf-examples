#!/bin/bash
<% @importAdded.each_with_index do|(key, value), index| -%>
ret="1"
while [ $ret -ne 0 ]
do
 /usr/bin/mongostat --host <%= value['ip'] %> --port <%= value['port'] %> -n 1 | grep connected
 ret=$? 
 /bin/sleep 2
done
<% end -%>
