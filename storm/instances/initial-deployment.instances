####################################
# We start with a simple deployment
####################################

# A VM with influxdb
instanceof VM_EC2 {
	name: ClusterStorm;
	
	instanceof storm {
		name: storm;
	}
}

